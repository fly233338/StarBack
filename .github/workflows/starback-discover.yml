name: StarBack Discover

on:
  watch:
    types: [started]
  schedule:
    - cron: "17 0 1 * *"

permissions:
  contents: read
  issues: write

jobs:
  discover:
    name: Discover repositories
    uses: fly233338/StarBack/.github/workflows/reusable-discover.yml@main
